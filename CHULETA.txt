///////ELIMINAR Y CARGAR LOS SEEDERS

php artisan migrate:fresh
php artisan db:seed



//Levantar docker

docker compose up -d --build

////////Ver variable que entra

@dd($ofertas)

/////////GIT---

git pull 

git add .
git commit -m "Describe el cambio que hiciste"
git push

//////////COLUMNAS NECESARIAS CSV DE AWIN

search_price
merchant_deep_link
delivery_cost
in_stock

/////////ACTUALIZAR TAILWIND - HAY QUE METERLO EN AMBAS CARPETAS PUBLIC/BUILD

npm run build

En Docker ---- docker compose run --rm node npm run build


////////OFERTAS CON AVISOS CON MENSAJE "GENERADO AUTOMATICAMENTE"

    ////para ver las ofertas


        SELECT
            op.*
        FROM
            ofertas_producto AS op
        INNER JOIN
            avisos AS a ON op.id = a.avisoable_id
        WHERE
            a.texto_aviso LIKE '%GENERADO%';

    /////para cambiar las ofertas a monstrar->si que tienen un aviso con la palabra GENERADO

        UPDATE
            ofertas_producto AS op
        INNER JOIN
            avisos AS a ON op.id = a.avisoable_id
        SET
            op.mostrar = 'si'
        WHERE
            a.texto_aviso LIKE '%GENERADO%';

    //eliminar los avisos con la palabra GENERADO

    DELETE FROM avisos WHERE texto_aviso LIKE '%GENERADO%';


////////////////////ELIMINAR IP ESPECIFICAS DE CLICKS
DELETE FROM clicks WHERE ip = 'IP_AQUI';




//////PARA DESPLEGAR DOCKER EN EL SERVIDOR

Para producción (en servidor):
bash
Copiar
Editar
npm install && npm run build
docker-compose -f docker-compose.prod.yml up -d --build
Navega a: http://<tu-ip-servidor>

Código no se expone como volumen

Laravel y Nginx sirven la web

//docker

    docker-compose up -d
    docker-compose down 

    //REINICIAR EL DOCKER
    docker-compose restart node

docker-compose exec node npm run dev

docker-compose exec app php artisan migrate
docker-compose exec app php artisan db:seed

IMPORTANTE!!   //apagar docker sin eliminar modulos
   docker-compose down

******* AWIN ***********

   Hola,

Sería un placer unirme a vuestro programa de afiliados y poder incluir !!!!!*/*/*/*/NOMBRE/*/*/*/*/!!!!!! a nuestro komparador.com

Considero que vuestra tienda ofrece un gran servicio y un catálogo muy interesante, y por los precios con los que trabajáis entraríais en las primeras posiciones en una gran cantidad de productos, por lo que creo que sería una buena incorporación para nuestros usuarios.

Si consideráis que encajamos con vuestro programa, nos encantaría colaborar en vuestro programa de afiliación.

Muchas gracias por vuestro tiempo y un saludo!